# DB Settings
DATABASE_HOSTNAME="db-dev" # put in the docker service name (e.g. db-dev) for the dev environment, and db for the prod environment
DATABASE_PORT="5434" # external db port
DATABASE_PASSWORD=""
DATABASE_USERNAME=""
DATABASE_NAME="agora"

# API Settings
API_PORT=8003 # external API port for the backend
SECRET_KEY = ""
ALGORITHM="HS256"
ACCESS_TOKEN_EXPIRE_MINUTES=760
REFRESH_TOKEN_EXPIRE_MINUTES=1000
ALLOWED_ORIGINS="http://localhost:5173,https://agora.dcs.hcu-hamburg.de" # create a comma-separated list of allowed origins for CORS, or add '*' to allow all origins

# Geoserver Settings
GEOSERVER_ADMIN_USER=admin
GEOSERVER_HOSTNAME="geoserver-dev" # put in the docker service name (as above)
GEOSERVER_ADMIN_PASSWORD=""
GEOSERVER_PORT=8383
METABASE_DB_PASSWORD=""